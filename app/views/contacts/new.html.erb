<%= render "shared/page_header",
  breadcrumbs: [["Companies", companies_path], [@company.name, company_path(@company)], ["Add Contact", nil]],
  title: "Add Contact" %>

<div class="card max-w-2xl">
  <div class="card-header">
    <h2 class="text-sm font-semibold text-slate-900">New contact for <%= @company.name %></h2>
    <p class="text-xs text-slate-500 mt-0.5">This contact will be added to the pipeline automatically.</p>
  </div>
  <div class="card-body">
    <%= form_with(model: [@company, @contact], class: "space-y-4") do |f| %>
      <% if @contact.errors.any? %>
        <div class="bg-rose-50 border border-rose-200 text-rose-800 px-4 py-3 rounded-lg text-sm">
          <ul class="list-disc list-inside">
            <% @contact.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div>
        <label class="form-label">Name</label>
        <%= f.text_field :name, class: "form-input" %>
      </div>

      <div>
        <label class="form-label">Email</label>
        <%= f.email_field :email, class: "form-input", required: true %>
      </div>

      <div>
        <label class="form-label">Role</label>
        <%= f.text_field :role, class: "form-input", placeholder: "e.g. CTO, VP Sales" %>
        <p class="text-xs text-slate-400 mt-1">The contact's job title or role at the company.</p>
      </div>

      <div class="flex gap-3 pt-2 border-t border-slate-100">
        <%= f.submit "Create Contact", class: "btn-primary cursor-pointer" %>
        <%= link_to "Cancel", @company, class: "btn-secondary" %>
      </div>
    <% end %>
  </div>
</div>
