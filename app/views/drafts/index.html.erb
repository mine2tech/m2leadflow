<% content_for(:actions) do %>
  <%= link_to new_draft_path, class: "btn-primary" do %>
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/></svg>
    New Email
  <% end %>
<% end %>
<%= render "shared/page_header",
  title: "Drafts",
  breadcrumbs: [["Drafts", nil]],
  actions: content_for(:actions) %>

<%# Status filter tabs %>
<div class="flex items-center gap-1 mb-6 bg-slate-100 rounded-lg p-1 w-fit">
  <% tabs = [["All", drafts_path, params[:status].blank?],
             ["Draft", drafts_path(status: :draft), params[:status] == "draft"],
             ["Reviewed", drafts_path(status: :reviewed), params[:status] == "reviewed"],
             ["Scheduled", drafts_path(status: :scheduled), params[:status] == "scheduled"],
             ["Sent", drafts_path(status: :sent), params[:status] == "sent"]] %>
  <% tabs.each do |label, path, active| %>
    <%= link_to label, path, class: "px-3 py-1.5 text-sm font-medium rounded-md transition-colors #{active ? 'bg-white text-slate-900 shadow-sm' : 'text-slate-600 hover:text-slate-900'}" %>
  <% end %>
</div>

<% if @drafts.any? %>
  <% show_bulk = params[:status] == "draft" %>
  <div data-controller="<%= 'bulk-select' if show_bulk %>">
    <% if show_bulk %>
      <div data-bulk-select-target="toolbar" class="hidden mb-4 flex items-center gap-3 bg-indigo-50 border border-indigo-200 rounded-lg px-4 py-2.5">
        <span class="text-sm text-indigo-700 font-medium">
          <span data-bulk-select-target="count">0</span> selected
        </span>
        <%= form_with url: bulk_approve_drafts_path, method: :post, class: "inline-flex", data: { action: "submit->bulk-select#submitSelected" } do %>
          <input type="hidden" name="draft_ids" value="" data-bulk-select-target="idsField">
          <button type="submit" class="btn-primary btn-sm" data-turbo-submits-with="Reviewing...">Review Selected</button>
        <% end %>
      </div>
      <div class="flex items-center gap-2 mb-3 pl-1">
        <input type="checkbox" data-bulk-select-target="selectAll" data-action="bulk-select#toggleAll"
               class="rounded border-slate-300 text-indigo-600 focus:ring-indigo-500">
        <span class="text-xs text-slate-500">Select all</span>
      </div>
    <% end %>
    <div class="grid gap-4">
      <% @drafts.each do |draft| %>
        <%= render "drafts/draft_row", draft: draft, show_checkbox: show_bulk %>
      <% end %>
    </div>
  </div>
<% else %>
  <div class="card">
    <%= render "shared/empty_state",
      title: "No drafts found",
      description: "Drafts are created automatically when contacts are added, or compose a new email.",
      icon: '<svg class="empty-state-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/></svg>',
      cta_text: "New Email",
      cta_path: new_draft_path %>
  </div>
<% end %>
